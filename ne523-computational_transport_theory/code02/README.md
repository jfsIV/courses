# Compile
- run `make` to compile
- run `make clean && make` to recompile

# Execution
- run `./sn_2d <input_file>` to execute `test` & `ddsolve`

If you do not enter any command line arguments, `test.in` will be used as the input.
